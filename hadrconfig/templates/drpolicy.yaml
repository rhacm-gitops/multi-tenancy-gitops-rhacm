apiVersion: ramendr.openshift.io/v1alpha1
kind: DRPolicy
metadata:
  name: odr-policy-5m
spec:
  drClusterSet:
    - name: "{{ include "primaryClusters" . }}"
      s3ProfileName: s3-primary
      region: "placeholder"
    - name: "{{ include "secondaryClusters" . }}"
      s3ProfileName: s3-secondary
      region: "placeholder"
  schedulingInterval: {{ .Values.schedulingInterval }}
